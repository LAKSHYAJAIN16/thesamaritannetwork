import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "The Samaritan Network - Connecting Hearts, Changing Lives",
  description: "Join The Samaritan Network charity app to make a meaningful impact. Connect with causes, donate securely, and track your contributions to help those in need.",
  keywords: "charity, donation, volunteer, nonprofit, community, giving, impact",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
